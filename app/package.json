{
  "name": "project-setup-bot",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub App that auto-creates projects when repos are created",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
